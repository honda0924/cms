%h2 顧客区分
= link_to '新規追加',new_payment_day_path
%p.client-rank
  - @payment_days.each do |p|
    %ul
      %li
        =p.date
      %li
        = link_to '編集',edit_payment_day_path(p.id)
        = link_to '削除',payment_day_path(p.id), method: :delete