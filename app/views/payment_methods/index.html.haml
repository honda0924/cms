%h2 顧客区分
= link_to '新規追加',new_payment_method_path
%p.client-rank
  - @payment_methods.each do |p|
    %ul
      %li
        =p.method
      %li
        = link_to '編集',edit_payment_method_path(c.id)
        = link_to '削除',payment_method_path(p.id), method: :delete